[package]
name = "common"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_yaml = "0.9"
glob = "0.3"
byteorder = "1.4"
bincode = { version = "2.0.0-rc.1", features = ["serde"]}
